<UserControl x:Class="PizzaStore.Presentation.Views.ManagePizzas.ManagePizzasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900">

    <Grid Background="#FAFAFA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Manage Pizzas"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryHueMidBrush}" />

        <!-- Pizza Form -->
        <StackPanel Grid.Row="1" Margin="0,20,0,0" Orientation="Vertical" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox Width="200"
                         Margin="5"
                         materialDesign:HintAssist.Hint="Pizza Name" />

                <ComboBox Width="200"
                          Margin="5"
                          materialDesign:HintAssist.Hint="Select Size">
                    <ComboBoxItem Content="Small"/>
                    <ComboBoxItem Content="Medium"/>
                    <ComboBoxItem Content="Large"/>
                </ComboBox>

                <TextBox Width="150"
                         Margin="5"
                         materialDesign:HintAssist.Hint="Price" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="Add Pizza"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="5"/>
                <Button Content="Update Pizza"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="5"/>
                <Button Content="Delete Pizza"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="5"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="0,10,0,0"
                      materialDesign:DataGridAssist.CellPadding="8"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="Auto"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
